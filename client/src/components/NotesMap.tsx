import { Box } from '@chakra-ui/react';
import React from 'react';

const NotesMap = () => {
  return (
    <Box bg={'red'}>
      <span>COMING SOON</span>
    </Box>
  );
};

export default NotesMap;
